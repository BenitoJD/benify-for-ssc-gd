{
  "name": "benify",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace benify-web",
    "build:web": "npm run build --workspace benify-web",
    "test:web": "npm run test --workspace benify-web",
    "lint:web": "npm run lint --workspace benify-web",
    "dev": "docker compose up --build"
  }
}
